<!DOCTYPE html>
<html>
  <head>
    <title>RailsBodega</title>


  </head>

  <body>
    <a href="<%= root_url %>">Home</a>
    <% if current_user %>
    <a href="<%= purchases_path %>">Purchases</a>
    <a rel="nofollow" data-method="delete" href="<%= signout_path %>">Log out</a>
    <% else %>
    <a href="<%= new_user_path %>">Sign Up</a>
    <a href="<%= signin_path %>">Log in</a>
    <% end %>



    <div class="container" role="main">
    <%= yield %>
  </div>
  </body>
</html>
